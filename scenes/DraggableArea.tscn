[gd_scene load_steps=3 format=2]

[sub_resource type="GDScript" id=2]
script/source = "extends Area


# Declare member variables here. Examples:
# var a = 2
# var b = \"text\"


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass


func _on_Area_mouse_entered():
	print(\"entered \" + self.name)


func _on_Area_mouse_exited():
	print(\"exited \" + self.name)


func _on_Area_input_event(camera, event, position, normal, shape_idx):
	var rounded_pos = Vector3(stepify(position.x, 0.01), stepify(position.y, 0.01), stepify(position.z, 0.01))
	if rounded_pos.length() > 3:
		return
	print(rounded_pos)
"

[sub_resource type="BoxShape" id=1]
extents = Vector3( 1.5, 1.5, 1.5 )

[node name="Area" type="Area"]
script = SubResource( 2 )

[node name="CollisionShape" type="CollisionShape" parent="."]
shape = SubResource( 1 )

[connection signal="input_event" from="." to="." method="_on_Area_input_event"]
